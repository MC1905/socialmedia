import { View, Text, Stylesheet, Textinput, Toucha} from 'react-native'
import React, { usetate} from 'react';
import { firebase} from '../config';

// const getPosts = () => {
//     const collectionRef = collection(db, "posts");
// }